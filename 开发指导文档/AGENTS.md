# AGENTS.md — 本项目的协作约束

> **每次开始写代码前先读这份。这是规矩的唯一权威来源。**
>
> | 文档 | 回答什么 | 什么时候读 |
> |---|---|---|
> | `PROJECT.md` | **为什么**——定位、理念、风险 | 开新对话时读一次 |
> | **AGENTS.md**（本文） | **做什么 / 不做什么**——所有规矩 | 每次 coding 都读 |
> | `ROADMAP.md` | **什么时候做、怎么算合格** | 每次 coding 都读 |
>
> 本文只讲规矩，不讲理由——某条规矩为什么存在，去 `PROJECT.md` 找。
> 做到哪一步、这一步的验收标准，去 `ROADMAP.md` 找。
>
> **这些规矩优先于你的默认习惯。任何冲突，以本文为准。**

---

## 0. 四句话背景

1. **这是一本特殊的 CS 教材。**网站里的课程是「目录」，**项目源代码和代码注释才是「正文」**，习题用课程官方的 lab / project。
2. **注释是一封写给未来读者的信。**语气像信，深度像教材，假设收信人零基础。
3. **课程目录照 csdiy.wiki 的分类做成一棵树，项目不内置学习路径**——路径由使用者自己组。
4. **作者不写代码，全程 AI 完成。**这是设计前提，不是待解决的问题。

---

## 1. 注释：本项目最重要的规矩

**注释不是代码的附属说明，注释是这本教材的正文。**

### 1.1 每个核心文件顶部必须有结构化注释块

字段名保持一致（构建时要扫描它）：

```
/**
 * @module        这个文件是什么、干什么用的
 * @problem       它解决什么问题（不写"怎么做"，写"为什么需要它"）
 * @design        为什么这样设计，考虑过哪些别的做法
 * @courses       对应哪些课程的哪些知识 —— 尽量列全，见 1.4
 * @exercises     哪些课程官方作业能练到这块（给链接）
 * @prereq        读懂这里需要先学什么
 * @unclear       哪里还没讲透 / 还没被验证
 *
 * @letter        写给未来读者的正文（见 1.2、1.3）
 */
```

`@letter` 是这个块里最重要的一段，不要省略、不要只写一句敷衍。

### 1.2 语气：一封信，不是技术文档

收信人是**未来那个学完了这门课、回过头来读这段代码的人**——
可能是作者自己，也可能是任何一个 fork 走项目的自学者。

用第一人称写给"你"：

> ❌ "此处采用递归下降解析以处理运算符优先级。"
>
> ✅ "写这段的时候我还没学到这部分，AI 说要分成两步才能处理优先级。
>    你现在学完 CS61A 的解释器项目了，回头看看它说得对不对——
>    如果你觉得有更好的写法，那就是这本教材该被改的地方。"

**信的语气不是装饰，是这本教材的教学法**：读者读到的是一条真实的认知路径，
而不是一个整理干净的结论。

### 1.3 深度：按教材写，假设收信人零基础

写信的语气，教材的标准。两者不冲突：

- 能用大白话说清的不堆术语；必须用术语时当场解释一句
- **讲"为什么"重于讲"怎么做"**——"怎么做"代码本身就写着了
- **诚实标注不确定**："这里为什么这样做我们没讲透，学完 X 再回来看"，
  比含糊带过有价值得多。这些地方将来正是这本教材最该被补上的章节

### 1.4 课程对应要列全，不要只列一条路线

项目的课程范围是 csdiy.wiki 那个规模的整个体系，不是作者当前在学的那几门。

一段代码如果同时和 CS61A、Stanford CS143、UCB CS164 有关，**三个都标上**。
读者不管沿哪条自建路径走过来，都应该能找到对应的代码。

不确定对应哪门课就写 `unknown`，别硬凑。

### 1.5 关键代码要有行内解释

不是每行都写。凡是"为什么这么做不那么做"的地方，写一句。
判断标准：一个刚学完对应课程的人读到这里会不会卡住。

### 1.6 每完成一个功能，主动提醒补注释

固定动作，作者不说你也要提。
这一步不影响功能能不能跑，所以最容易被跳过——但跳过了，项目就退化成一个普通网站。

---

## 2. 课程与路径

### 2.1 课程目录照搬 csdiy.wiki 的分类

做成一棵分类树，形态就像那个网站的侧边栏：必学工具、编程入门、数学基础、
数据结构与算法、系统（操作系统 / 编译 / 网络 / 数据库 / 体系结构）、
人工智能与机器学习、理论计算机科学等。

**同一分支下并存多所学校的同类课程**（操作系统那一支就有好几套），这是原始设计，要保留。

每门课的页面放：课程简介、先修要求、官方资源链接、官方作业链接、对应的项目模块。

### 2.2 项目不内置学习路径

**项目提供的是这棵树，不是一条路线。**

读者读完课程介绍自己决定学什么、按什么顺序学。路径由使用者自己组，
**属于访问者状态**（存他自己的浏览器），不是项目内容的一部分。

作者本人目前选的是 learncs.site 那条（CS50x → CS61A/B/C → …，配 MIT 数学课）。
这只是**作者的个人选择**，可以作为示例展示，但**不是默认路线，也不是课程目录的边界**。

### 2.3 数据结构要求

- 课程树：内容状态，跟项目走
- 学习路径：使用者在树上勾选和排序出来的，访问者状态
- **不要把任何一条路线的顺序硬编码进课程数据里**
- 一门课可以出现在任意多条使用者自建的路径中

---

## 3. 习题：一律用课程官方的

**不要自己编练习题。**

课程页和代码讲解页上的"练习"，一律指向对应课程的官方 lab / project / problem set，
给出链接和简短说明（这个作业练的是本模块的哪部分）。

理由：官方作业有完整框架和保姆级说明，质量远高于自编题。
项目要做的是把它们**组织好、链接好**，不是重新发明。

---

## 4. 沟通方式

### 4.1 用大白话解释，术语当场说明

- ✅ "这段是把用户输的那行字按空格拆开，第一个词当命令名"
- ❌ "这里做了词法分析，把输入流切分成 token 序列"

**反正这些解释最后都要写进注释——注释就是教材正文。**

### 4.2 不要劝作者自己写代码

不要说"这部分建议你自己实现"，不要留"故意写简单、以后自己重写"的代码。
不要为了迁就作者当前的学习进度而简化架构或降低教材深度。

### 4.3 作者问"这是什么"是正常流程

回答好，**并且顺手把答案写进对应文件的注释里**。

### 4.4 回答要短

先给结论，再给必要解释。不要长篇铺垫，不要一件事说三遍。

### 4.5 不确定就问

需求不清楚时停下来问一句，比猜错方向写一百行强。

---

## 5. 每次改动的节奏

### 5.1 一次只做一件能被亲眼验证的事

粒度应该是这种：

- "做一个空白页面，中间有输入框，回车后把内容显示在上面"
- "让输入 `ls` 显示三行固定文字"
- "把这三行改成从一个列表读出来"
- "让 `cd cs` 能进去"

**如果一步做完作者没法亲眼确认它成了，那这步太大。**
不要一次性交付"一个完整的终端系统"。

### 5.2 每次改动后必须说清三件事

1. 改了哪些文件
2. 应该看到什么现象（怎么验证成没成）
3. 下一步建议做什么

### 5.3 完成一块就提醒存档

功能跑通就提醒提交一次。理由说清：AI 有时会把好好的东西改坏，有存档才能退回去。

---

## 6. 不要做的事

### 6.1 不要自作主张加功能

只做要求的。想到别的点子，**说出来问，不要直接写**。

### 6.2 不要换技术

不要把现有的库换成"更好的"，不要引入没商量过的新依赖。真觉得该换，先说理由。

### 6.3 不要美化在功能之前

第一版打通之前，不碰配色、动画、字体、过渡效果。
**这类项目最常见的死法就是界面很漂亮，点进去什么都没有。**

### 6.4 不要用 xterm.js

终端界面用普通网页组件实现（一个输入行 + 一组输出组件）。
原因：本项目不接真实 shell，用不上 xterm 的核心能力，反而受限于"输出只能是纯文本"。
而这里需要 `ls` 每一项可点、进度条是真进度条、目录树能折叠、代码块能高亮。

### 6.5 不要因为课程页面内容少就提醒补内容

课程页只放简介、官方资源链接、官方习题链接就够了。**正文在代码注释里。**

### 6.6 不要写"总结报告"式的回复

不要附一大段"我完成了以下工作：1... 2... 3..."。说清改了什么、怎么验证就够。

---

## 7. 代码结构约束

### 7.1 命令引擎必须和框架无关

处理命令的代码（拆解输入、执行、产出结果）**不能引用 React、Next 或任何浏览器 API**。
它就是"输入一行字、输出一组结果"的纯逻辑模块，能脱离网页单独测试。

需要跳转、需要存数据时，**命令不直接做，而是返回一个"我想做这件事"的描述**，
由外层统一执行。

这块是教材里最重要的一章（对应 CS61A 解释器、编译原理等课程），
所以边界要守住，注释要写得比别处更细。

### 7.2 页面地址是唯一真相

终端和鼠标**不互相通知**，都读同一个页面地址：

- 终端的 `cd` / `open`：算出目标地址 → 跳转
- 侧边栏高亮、终端提示符：从当前地址反推

**终端不要自己存一份"当前目录"。**

### 7.3 命令输出是结构化数据，不是拼好的字符串

命令返回一组带类型的结果（文本 / 链接 / 列表 / 树 / 进度条 / 表格 / 代码块），
界面层负责渲染。

命令之间传数据也传结构化对象——后一个命令不应该需要解析前一个命令的文本格式。

### 7.4 三种状态严格分开

| 类型 | 存哪 | 例子 |
|---|---|---|
| 内容状态 | 内容文件，跟项目走 | 课程树、课程简介、前置关系、**作者的进度和心得** |
| 会话状态 | 内存，刷新即丢 | 命令历史、终端输出 |
| 访问者状态 | 访问者浏览器 | **他自建的学习路径**、待办、心得、理解度 |

**特别注意**：首页的"完成 81%"是**作者的**进度，人人可见；访问者勾的待办是另一套。
从一开始就用不同的命令、不同的存储，不要合并。

### 7.5 新文件放哪要先想清楚

不确定放哪个目录就问一句，不要随手扔根目录。目录名一律用英文。

---

## 8. 终端行为约束

### 8.1 命令语义严格对齐真 Unix

读者在这里养成的习惯将来要迁移到真终端——**这本身就是教材内容**
（对应 csdiy.wiki「必学工具」那一章）。所以：

- `ls` `cd` `cat` `pwd` `tree` `find` `grep` `history` 行为和真的一致
- 支持 `cd ..`、`cd -`、`~` 展开、Tab 补全、`↑↓` 翻历史
- **不要发明方便但非标准的语法**。能用 `cat` 表达的就不要新造词。

### 8.2 报错信息照抄 Unix

```
cd: no such file or directory: xxx
```

**不要做"你是不是想输入 xxx"这种自动纠正**——那会让人放松，而放松学不会命令行。

### 8.3 项目自己的命令另算

路径管理、心得、待办、按课程反查代码这类命令可以自己定义，
但命名风格和 Unix 保持一致：小写、简短、动词优先。

### 8.4 首页的终端反过来要极度友善

首页那个演示终端面向陌生访客：有提示、有可点的示例命令、`help` 一目了然。
**同一个内核，两种默认友善度。**

---

## 9. 部署约束

**第一版做纯静态导出，部署到 GitHub Pages。不要使用任何需要服务端的功能。**

公开评论用 GitHub Discussions 方案（Giscus），仍然是纯静态。

将来真需要账号 / 云同步 / AI 问答时再迁到 Vercel 免费档——那时改配置即可，不用重写。
**在作者明确说要迁之前，一切按纯静态设计。**

---

## 10. 判断优先级的标准

不确定某件事该不该做时，按顺序问：

1. **它让「目录 → 正文 → 习题」的连接更紧密吗？**（注释、双向跳转、官方习题链接、理解度）→ 优先做
2. **它是终端能做而鼠标做不到的事吗？**（跨课查询、按状态筛选、组合命令）→ 值得做
3. **它只是让终端更像终端吗？**（更多命令、更炫的输出）→ 往后放
4. **它是美化吗？** → 第一版打通之前一律不做

---

## 11. 做到哪一步：看 ROADMAP.md

**当前该做哪一步、这一步做到哪里为止、怎么算合格，一律以 `ROADMAP.md` 为准。**
本文不重复阶段划分和验收标准。

三条硬性纪律：

- **只做作者指定的那一步，不要往前做。**
- **动手前先说清"这一步做完作者应该看到什么现象"**，等确认后再开始。
- **界面上看不见结果的步骤**（索引、虚拟文件系统等），必须提供一个临时的可见出口
  供作者验收，确认后再删掉。不要让作者接受"你放心，跑通了"。
